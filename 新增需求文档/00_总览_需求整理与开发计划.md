# 00 · 总览 —— 需求整理与开发计划

> 本文是**【周五课堂新增需求】文档集的总入口**。各功能细节见对应分册（见文末索引）。
> 合订版（全文不分册）保留在 `周五课堂_新增需求_开发计划书_V1.0.md`，内容与本套分册一致。

| 项 | 内容 |
|---|---|
| 项目 | 周五课堂（Friday Class）— 智能教学互动平台 |
| 版本 | V1.0 |
| 日期 | 2026-09-20 |
| 代码库 | `Friday Class(Finish)`（由 MVP 迁移而来，见 §4） |
| 前置基线 | MVP 已完成 F001–F004，`main` = `f0e872c` |

---

## 一、本文档集要解决什么

Friday 一次性提出了 8 条新需求，**顺序是乱的、颗粒度不一、彼此有隐含依赖**。本套文档做三件事：

1. **整理需求顺序** —— 把 8 条原始需求重排成有依赖关系的 7 个模块（见 §2）；
2. **数据库建模** —— 新增 4 张表、修改 3 张表，含完整可执行 DDL（见 `01_数据库建模.md`）；
3. **逐模块的接口设计 + 技术方案 + 开发步骤 + 验收标准**（见 `M1` ～ `M7` 分册）。

---

## 二、需求整理（把乱序的 8 条重排）

### 2.1 原始 8 条 → 模块归类

| 原始编号 | 原始描述 | 归入模块 |
|---|---|---|
| 5 | 不要 PPT 同步，要学生能看到老师屏幕 + 传输音频 | **M1 实时课堂传输** |
| 2 | 课堂上需要打字发言讨论区 | **M2 课堂讨论区** |
| 6 | 学生看不到 PPT 经 AI 解析后的内容，学生也要看到 | **M3 学生端知识可见** |
| 8 | 学生下课也能用 AI；AI 不能每次登录都是新的；要会话管理和上下文记忆 | **M4 学生 AI 会话** |
| 3 | 记录课堂内容：发言区内容 + 各学生与 AI 的聊天记录（没聊的不显示） | **M5 课堂记录** |
| 4 | 课堂总结功能（仅针对聊天记录） | **M5 课堂记录** |
| 1 | 管理端：管理课堂、账号、存储的数据，随时暂停所有课堂等 | **M6 管理端** |
| 7 | 更好的 UI 界面（CSS，可后置） | **M7 界面美化** |

### 2.2 依赖关系（为什么必须这个顺序）

```
        ┌──────────────────────────────────────────┐
        │  P0 地基（无可见变化）                     │
        │  · 新建 4 表 / 改 3 表                    │
        │  · ADMIN 角色                             │
        │  · WebSocket 升级为双向 + 带用户身份       │
        │  · CSS 设计令牌                           │
        └───────────────┬──────────────────────────┘
                        │
        ┌───────────────┴──────────────────────────┐
        │  P1 实时核心   M1 屏幕+音频   M2 讨论区    │  ← 技术不确定点，先做
        └───────────────┬──────────────────────────┘
                        │
        ┌───────────────┴──────────────────────────┐
        │  P2 学生 AI    M4 会话    M3 知识可见      │
        └───────────────┬──────────────────────────┘
                        │
        ┌───────────────┴──────────────────────────┐
        │  P3 记录总结   M5（依赖 M2 的发言 + M4 的AI记录）
        └───────────────┬──────────────────────────┘
                        │
        ┌───────────────┴──────────────────────────┐
        │  P4 管理端     M6（依赖 P0 的 ADMIN 角色 + P1 的暂停状态）
        └───────────────┬──────────────────────────┘
                        │
        ┌───────────────┴──────────────────────────┐
        │  P5 UI 收口    M7（套用 P0 令牌，回填老页面）
        └──────────────────────────────────────────┘
```

**排序理由（说人话）：**

- **P0 必须最先**：后面每个模块都要用新表、新角色、新 WebSocket 能力。地基不做，上面全是空中楼阁。
- **P1 排在前面**：屏幕传输是**唯一有技术不确定性**的部分。万一有坑，越早发现越好，不能拖到最后。
- **P2 在 P3 之前**：课堂记录要记录"学生和 AI 聊了什么"，先得有会话体系。
- **P4 在 P1 之后**：管理端的"暂停所有课堂"依赖 P1 定义好的课堂状态机。
- **P5 最后**：UI 是横切关注点，等页面都长出来了再统一回填，避免返工。

### 2.3 模块总览

| 模块 | 名称 | 来源需求 | 阶段 | 规模 | 分册 |
|---|---|---|---|---|---|
| M1 | 实时课堂传输（屏幕 + 麦克风音频） | 5 | P1 | 中 | `M1_实时课堂传输.md` |
| M2 | 课堂讨论区 | 2 | P1 | 中 | `M2_课堂讨论区.md` |
| M3 | 学生端知识可见 | 6 | P2 | 小 | `M3_学生端知识可见.md` |
| M4 | 学生 AI 会话与上下文 | 8 | P2 | 中 | `M4_学生AI会话与上下文.md` |
| M5 | 课堂记录与总结 | 3 + 4 | P3 | 中 | `M5_课堂记录与总结.md` |
| M6 | 管理端 | 1 | P4 | 大（活多不难） | `M6_管理端.md` |
| M7 | 界面美化 | 7 | P5 | 中 | `M7_界面美化.md` |

---

## 三、已确认的边界条件（前置约束，不再讨论）

| 项 | 决定 | 影响 |
|---|---|---|
| 演示环境 | **只用 localhost** | 屏幕共享无障碍（`getDisplayMedia` 仅安全上下文可用，localhost 属安全上下文） |
| 音频 | **只要老师麦克风**，不要 PPT/系统声音 | 绕过 Chrome 系统音频捕获限制 |
| 并发规模 | **最多 2 个学生接收** | 不做多路扇出优化、不引入 SFU/媒体服务器 |
| 对话长度 | 不演示长对话 | 上下文只做**窗口兜底**，不做摘要压缩 |
| 聊天记录量 | 不演示大量记录 | 总结**单次调用**，不做分块 map-reduce |
| 管理端范围 | 用户 / 课堂 / 课件与存储 **三组**；AI 成本统计**删除** | 审计日志按指示**保留** |
| 测试 | 不搞全项目覆盖，只写**验证新功能的脚本** | 不做漏洞彻查 |
| 端口 | 前端固定 **5173** | 后端 CORS 不动 |
| 数据迁移 | **不搬任何老数据**，只加新表新列 | 老记录原样保留 |
| 省钱优化 | **全部删除**（答案复用 + 并发合流） | 消除上下文串台问题 |
| WebRTC 方案 | **纯 P2P，不新增独立服务** | 信令复用现有 WebSocket |

### 术语速查（给不写代码的人看）

| 术语 | 一句话解释 |
|---|---|
| **P2P** | 点对点直连。老师的浏览器直接把画面传给学生的浏览器，不经过服务器中转 |
| **信令** | 建立直连前的"交换名片"过程（互相告知地址和能力）。我们用现有 WebSocket 承担 |
| **SFU** | 媒体服务器。腾讯会议用的方案，所有流经过服务器中转分发。**本项目不用** |
| **幂等** | 同一个操作执行多次，结果和执行一次相同。数据库迁移脚本必须幂等 |
| **软删除** | 不真删数据，只把 `deleted` 标记置 1。本项目 `user`/`courseware`/`class_session` 都用这个模式 |

---

## 四、代码库迁移说明（MVP → Finish）

本次已将代码从 MVP 目录迁移到 Finish 目录，作为**最终交付的工作目录**。

| 项 | 说明 |
|---|---|
| 来源 | `C:\Users\Friday\Desktop\Friday Project\Friday Class(MVP)` |
| 目标 | `C:\Users\Friday\Desktop\Friday Project\Friday Class(Finish)`（本目录的上级） |
| 方式 | 从 GitHub 克隆（保留完整提交历史），**再补齐被 gitignore 的运行必需文件** |
| 远端 | `https://github.com/FFFFriday/friday-class.git` |
| 分支 | `main`（= `f0e872c`） |

**为什么不能只克隆**：以下文件被 `.gitignore` 排除，克隆拿不到，缺了会导致**项目起不来或没有数据**：

| 补齐的文件 | 体量 | 缺了会怎样 |
|---|---|---|
| `backend/src/main/resources/application-local.yml` | 4 KB | 装着真实 DeepSeek Key + JWT 密钥，**缺了后端启动即报错** |
| `backend/storage/` | 25 MB | 课件源文件 + 生成的幻灯片，**缺了没有可演示的课件** |
| `项目看板.md` | 32 KB | MVP 里有未提交的更新（+120/−39），**克隆会丢** |

> ⚠️ 注意：`Friday Class(MVP)` 目录**仍然存在且可用**，本次是复制不是搬走。确认新目录没问题后可自行删除旧目录。
>
> 另外两件易漏的事：
> 1. 新目录**没有 `frontend/node_modules`**（45 MB，被 gitignore）——首次运行前需在 `frontend/` 执行 `npm install`。
> 2. 新目录**没有 `backend/target`**（Maven 编译产物）——首次运行会自动重新编译。

---

## 五、开发计划总表

### 5.1 阶段与交付

| 阶段 | 模块 | 交付物 | 依赖 | 规模 |
|---|---|---|---|---|
| **P0 地基** | — | 4 张新表 + 3 张表改列、ADMIN 角色、WS 双向升级、设计令牌和基础组件 | 无 | 中 |
| **P1 实时核心** | M1 + M2 | 老师屏幕+麦克风可被学生看到听到；讨论区可用 | P0 | **中（含技术验证）** |
| **P2 学生 AI** | M4 + M3 | 学生会话管理 + 上下文 + 课后可用；学生能看到知识点 | P0 | 中 |
| **P3 记录总结** | M5 | 课堂记录页 + 总结生成 | P1 + P2 | 中 |
| **P4 管理端** | M6 | 三组管理功能 + 审计日志 | P0 + P1 | **大（活多）** |
| **P5 UI 收口** | M7 | 全站统一视觉 | P0 | 中 |

### 5.2 关键路径

```
P0 ──► P1（技术验证是关键节点）──► P3 ──► P5
        │
        └──► P2 ──► P3
        │
        └──► P4
```

**P1 的 M1-1（技术验证）是整条路径上唯一的不确定点。**它不通，需要立即停下来重新商量方案（备选：退回 PPT 图片同步 + 只做音频）。

### 5.3 每个阶段的完成定义（DoD）

| 要求 | 说明 |
|---|---|
| 可演示 | 该阶段结束时，新增功能**当场能跑给人看** |
| 不破坏既有 | 原有 F001–F004 功能**全部仍然可用**（每次阶段结束做一次回归） |
| 有验证脚本 | 该阶段的新功能有对应的验证脚本，能一次跑完 |
| 文档同步 | 同步更新 `API接口文档.md`、`项目看板.md` |

---

## 六、验证方案

### 6.1 原则

按 Friday 指示：**不做全项目覆盖式测试，只写验证新功能的脚本**。但**改动过现有代码的地方必须有回归**。

### 6.2 必须验证的清单（优先级从高到低）

| # | 验证项 | 为什么必须 | 关联分册 |
|---|---|---|---|
| 1 | **学生 token 调 `/api/admin/**` → 403** | 权限漏配 = 删库风险 | M6 |
| 2 | **不同会话问同一问题 → 答案不同** | 证明删了缓存、上下文隔离生效 | M4 |
| 3 | **课后提问（无 pageId）能落库** | 证明 `page_id` 可空迁移成功 | M4 / 01 |
| 4 | **没跟 AI 聊过的学生不出现在记录里** | Friday 明确要求 | M5 |
| 5 | **讨论区发言含 `<script>` 时原样显示不执行** | XSS 安全 | M2 |
| 6 | **翻页功能未被 WS 改动破坏** | 动过核心链路 | M1 / 02 |
| 7 | **登录/鉴权未被 ADMIN 角色改动破坏** | 动过核心链路 | M6 |
| 8 | **老数据照常显示**（`conversation_id` 为 NULL 的记录） | 没有搬数据 | 01 |

### 6.3 验证脚本形式

- 后端：`backend/src/test/java/` 下的 JUnit 集成测试（**目前该目录是空的，P0 需建立**）
- 端到端：一个可重复执行的脚本，**一次跑完**（Friday 的既有要求：带时间窗的验证必须一个脚本一次跑完）

---

## 七、开放问题与已知限制

| # | 事项 | 状态 |
|---|---|---|
| 1 | 屏幕共享仅 localhost 可用（换局域网 IP 会失效） | **已接受**，演示固定 localhost |
| 2 | 音频只有麦克风，不含 PPT 内嵌声音 | **已接受** |
| 3 | 「暂停课堂」无法从服务端停掉老师的画面（P2P 固有特性） | **需在文档和界面中写明**（详见 M6） |
| 4 | `application-local.yml` 明文密钥 | **按 Friday 指示不换**，演示时注意不要投屏 |
| 5 | 测试账号 `demo_sd` / `sd123456` 仍在库中 | **P0 阶段顺手清理** |
| 6 | `项目看板.md` 有未提交改动（+120/−39） | 已随本次迁移带过来，仍处于未提交状态 |
| 7 | 老数据中的脏课件（`courseware` 4/5/6） | 验证时**避开**，不作为基准 |

---

## 八、决策速查

| 决策 | 结论 |
|---|---|
| 屏幕传输方案 | WebRTC P2P，信令走现有 WS，**不引入独立服务** |
| 页码来源 | 老师共享我们的网页幻灯片标签页 → 程序自动知道页码 |
| 音频来源 | 老师麦克风 |
| 并发规模 | ≤2 学生，不做 SFU |
| 省钱优化 | 全删（答案复用 + 并发合流） |
| 上下文窗口 | 6 轮兜底，不做压缩 |
| 总结分块 | 不做，超长直接截断 |
| AI 归属 | 会话归学生，消息打课堂/课件标签 |
| 管理端范围 | 用户/课堂/课件存储 三组；AI 成本统计删除 |
| 审计日志 | 保留 |
| 测试范围 | 只验证新功能，不做全面覆盖 |
| 端口/CORS | 5173 不动 |
| 数据迁移 | 不搬老数据，只加表加列 |
| 密钥 | 不更换 |

---

## 九、文档集索引

| 文件 | 内容 |
|---|---|
| **`00_总览_需求整理与开发计划.md`** | 本文。需求整理、计划总表、验证方案、决策速查 |
| `01_数据库建模.md` | 新增 4 表 + 改 3 表，完整 DDL 与幂等迁移脚本、关系图、索引说明 |
| `02_技术约定_WebSocket协议与权限.md` | WS 消息协议 v2 全表、权限模型、审计动作码、错误码 |
| `M1_实时课堂传输.md` | 屏幕 + 麦克风音频 P2P 传输、纯净演示页 |
| `M2_课堂讨论区.md` | 打字发言区、限流、删言、XSS 防护 |
| `M3_学生端知识可见.md` | 学生查看当前页知识点与预置提问 |
| `M4_学生AI会话与上下文.md` | 会话 CRUD、上下文记忆、课后可用、删除省钱两件套 |
| `M5_课堂记录与总结.md` | 课堂记录页（发言 + 分学生 AI 记录）、总结生成 |
| `M6_管理端.md` | 用户 / 课堂 / 课件与存储 三组管理 + 审计日志 |
| `M7_界面美化.md` | 设计令牌、基础组件、全站统一 |
| `Z_附录_文件清单.md` | 按阶段列出所有新增/修改文件 |
