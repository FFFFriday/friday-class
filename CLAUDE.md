# 项目宪章：周五课堂（Friday Class）— 智能教学互动平台（MVP 代码项目）

> 本文件是「周五课堂」**代码项目**的宪章，每次进入本项目自动加载，承载选题、定调、关键决策与长期规划。
> 配套的**文档项目**（需求调研 / PRD / 项目计划 / SRS）在 `C:\Users\Friday\Desktop\Shixun Project`，本文只保留代码实现必需的结论，文档细节不在此重复。

## 一、项目身份

- **产品名**：周五课堂（Friday Class）；主题：**智能教学互动平台**
- **选题**：`人工智能开发选题案例介绍.docx` 第 8 案例「智能互动教学平台」
- **团队**：组长 刘康旭（202324120310，即 Friday）；组员 张津玮（202324120336）
- **文档用单位名**：武汉学链科技有限公司
- **当前阶段**：由「文档交付」进入「代码实现」——先搭架构、建库、建仓库，**暂不写 MVP 业务**

## 二、一句话定位 + 核心闭环

教师上传 `.pptx` 课件 → AI 逐页抽取知识点并预生成提问 → 授课时录屏直播、翻页实时广播页码 → 学生端 AI 助手随页码加载对应页知识点、文字问答 → 课后基于「课件 + 知识点 + 问答记录」自动生成课程总结。

三段闭环：**课前课件解析 → 课中直播问答 → 课后自动总结**。

## 三、产品范围（六大功能 F001–F006）

| 编号 | 模块 | 分级 | 一句话 |
|---|---|---|---|
| F001 | 课件上传与解析 | A（Must） | `.pptx` → 网页幻灯片 + 逐页文字抽取 |
| F002 | AI 课件解析 | A（Must） | DeepSeek 逐页生成知识点 + 预置提问 |
| F003 | 直播授课与翻页同步 | A（Must） | 录屏直播 + 翻页广播页码 |
| F004 | 学生端 AI 问答助手 | A（Must） | 按当前页加载提示词，文字问答 |
| F005 | 课后总结归纳 | B（Should） | 课件 + 知识点 + 问答 → 自动总结 |
| F006 | 学情统计 | C（Could） | 问答记录聚合统计 |

**MVP 范围 = Must 四项（F001–F004）**：打通「上传课件 → 逐页解析 → 直播翻页 → 学生问答」主链；F005 / F006 在 MVP 之后补齐。

## 四、技术栈（选型定调，硬性）

| 层 | 选型 |
|---|---|
| 大模型 | **仅 DeepSeek 纯文本模型**（无音频 / 图像 / 多模态；具体版本与 API 接入待 plan 确认，成本敏感） |
| 前端 | Vue |
| 后端 | SpringBoot（课件上传存储 + pptx 转网页幻灯片 + 文字抽取 + 接口服务） |
| 数据库 | MySQL 9.5（本机已装，语法向后兼容 8.0；结构化数据） |
| 文件存储 | 服务器文件系统 / 对象存储（课件 `.pptx` + 网页幻灯片） |
| 实时 | WebSocket（翻页页码广播） |
| 认证 | JWT（无状态令牌，不用 Session / Redis） |
| 交付 | MVP（P0 核心）→ 完整版 |

## 五、关键设计决策（硬性约束，从文档阶段继承）

- **翻页同步机制**：学生端看到的是老师录屏直播画面（视频），AI 靠「页码」定位知识点。老师端 PPT 转成网页幻灯片在浏览器打开，翻页即检测并广播页码；学生端 AI 提示词随页码切换（提示词预先下发、存学生浏览器缓存）。
- **「知识图谱」名实分离**：对外文档仍称「知识图谱」（选题 / 创新点关键词），实现上是轻量「知识点列表 + 页面关联」，不碰 Neo4j 等重图数据库。
- **只用 DeepSeek 纯文本**：DeepSeek 无 TTS / ASR / 图像接口，故音频、实时语音、多模态全砍；课件限定「文字型」（图片课件需 OCR，违反单模型约束）。
- **已砍功能**：双 AI 音频课、AI 同学 / 多智能体协同、实时语音（豆包通话式）、思维导图 / 互动 HTML 教材 / PPT+旁白、个性化内容推荐、真·知识图谱。

## 六、数据库结构方向（Task 1 依据）

SRS 数据字典定义了 **7 个实体**，是建表的基线：

| # | 实体 | 建议表名 | 核心字段 |
|---|---|---|---|
| 1 | 用户 | `user` | 用户ID、用户名、密码哈希、角色（教师/学生）、昵称 |
| 2 | 课件 | `courseware` | 课件ID、名称、文件路径、上传者ID、上传时间 |
| 3 | 课件页 | `courseware_page` | 页ID、所属课件ID、页码、页文字、网页幻灯片地址 |
| 4 | 知识点 | `knowledge_point` | 知识点ID、所属页ID、知识点内容 |
| 5 | 预置提问 | `preset_question` | 提问ID、所属页ID、提问内容 |
| 6 | 问答记录 | `qa_record` | 问答ID、学生ID、所属页ID、问题、回答、提问时间 |
| 7 | 课程总结 | `course_summary` | 总结ID、所属课件ID、总结内容、生成时间 |

**要求**：支撑**整个项目**（不只为 MVP），且**具有扩展性**——Task 1 需先审查此数据字典是否有缺漏 / 不合理，再产出最终建表结构（建表 SQL）。

## 七、项目规范（宪章级，每次会话生效）

- **Word 文档格式对齐**：编辑已有 Word / WPS 文档时，必须沿用文档内已有文字的字体、字号与格式，不得改动原格式；同文档内字体 / 字号 / 段落 / 表格 / 标题层级一致（正文宋体、标题黑体）。
- **编码**：Windows 上处理 docx 用 `PYTHONIOENCODING=utf-8`，避免 GBK 乱码。
- **代码**（暂定，plan 阶段细化）：前端 / 后端分离，各自遵循生态约定。

## 八、关键文档指针（均在 Shixun Project）

| 文档 | 路径 |
|---|---|
| SRS | `1_周五课堂_Software Requirement Specification_V1.0.docx` |
| PRD | `周五课堂_PRD.md` |
| 项目概览 | `周五课堂_项目概览.md` |
| 项目计划 | `1_周五课堂_刘康旭_Software Project Planning(simple)_V1.0.docx` |
| 项目启动报告 | `1_周五课堂_Project Start Report_V1.0.docx` |

## 九、当前任务（2026-09-08）

1. **建库**：审查 SRS 数据字典 → 设计支撑整个项目、可扩展的数据库表结构（建表 SQL）。
2. **架构 + 仓库**：完成项目架构创建；建远程仓库（GitHub 已配置，Gitee 待配置，两处同时上传）。
3. **架构设计图**：产出架构设计图（XML 文件，最终以 Word 文档呈现）。
4. **初始化工程**：创建前端 / 后端文件夹，搭建项目骨架，**暂不开始 MVP 制作**。
