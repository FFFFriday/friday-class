# 周五课堂 · 项目总看板

> 一页看清：**做了什么、怎么实现的、还差什么、配置之间怎么牵扯**。
> 快速读懂代码见 `backend/README.md`；接口细节见 `docs/api-contract.md`；AI 设计见 `docs/ai-agent-design.md`。

**更新日期**：2026-09-11 ｜ **当前阶段**：MVP 核心链路开发中

---

## 一、一页速览

| 项 | 状态 |
|---|---|
| 需求与设计文档 | ✅ 完成 |
| 数据库（9 张表） | ✅ 完成 |
| 后端骨架 + 认证 | ✅ 完成 |
| 后端门户（课件浏览） | ✅ 完成 |
| **后端课件上传与解析** | ✅ 完成（F001） |
| AI 智能体设计 | ✅ 完成（文档 + 3 张图） |
| **AI 课件知识点解析** | ⬜ 待做（F002）🛑 **缺 DeepSeek API Key** |
| 直播翻页同步 | ⬜ 待做（F003） |
| 学生 AI 问答 | ⬜ 待做（F004） |
| 课后总结 | ⬜ 待做（F005） |
| 学情统计 | ⬜ 待做（F006） |
| 前端页面 | 🟡 骨架完成，**跑的是假数据** |
| 前后端真实联调 | ⬜ 待做 |

**现在就能打开看的东西**：

| 看什么 | 地址 |
|---|---|
| 门户（课件库） | `http://localhost:5173`（前端 dev server） |
| 网页幻灯片示例 | `http://localhost:8081/slides/8/page1.html` |
| 后端 API | `http://localhost:8081/api/...` |

---

## 二、功能总览（F001–F006）

| 编号 | 功能 | 分级 | 状态 | 用到的技术 | 主要代码位置 |
|---|---|---|---|---|---|
| **F001** | 课件上传与解析 | A 必做 | ✅ **完成** | Spring MVC 文件上传、Apache POI、UUID 落盘 | `controller/CoursewareController`、`service/CoursewareUploadService`、`service/PptxConverter`、`service/FileStorageService` |
| **F002** | AI 课件解析 | A 必做 | ⬜ 待做 | DeepSeek API、异步任务、指数退避重试 | 计划：`service/ParseAgentService`；实体 `AiParseTask` 已就位 |
| **F003** | 直播授课与翻页同步 | A 必做 | ⬜ 待做 | WebSocket、`class_session.current_page` | 计划：`config/WebSocketConfig`；表已备好 |
| **F004** | 学生端 AI 问答助手 | A 必做 | ⬜ 待做 | DeepSeek API、按页组装提示词 | 计划：`service/QaAgentService`；设计见 `docs/ai-agent-design.md` |
| **F005** | 课后总结归纳 | B 应做 | ⬜ 待做 | DeepSeek API、聚合课件+问答 | 表 `course_summary` 已备好 |
| **F006** | 学情统计 | C 可做 | ⬜ 待做 | 问答记录聚合查询 | 表 `qa_record` 已备好 |

### 非功能能力（已实现）

| 能力 | 状态 | 技术 | 位置 |
|---|---|---|---|
| 注册 / 登录 | ✅ | JWT（HS256）+ BCrypt | `service/AuthService`、`security/JwtService` |
| 登录态校验 | ✅ | 自定义过滤器 + 每次查库 | `security/JwtAuthenticationFilter` |
| 改密码即失效旧令牌 | ✅ | `user.token_version` 比对 | `AuthService.changePassword` |
| 角色权限（教师/学生） | ✅ | Spring Security 过滤链 + `@PreAuthorize` | `config/SecurityConfig` |
| 统一响应 / 全局异常 | ✅ | `@RestControllerAdvice` | `common/GlobalExceptionHandler` |
| 软删除 | ✅ | `@SQLRestriction` + 显式方法名 | `entity/Courseware` 等 |
| 课件列表分页 / 搜索 / 筛选 | ✅ | JPA + JPQL | `repository/CoursewareRepository` |
| 网页幻灯片生成 | ✅ | POI 抽文字 + 自建 HTML 模板 | `service/PptxConverter` |
| 文件上传安全 | ✅ | 扩展名白名单 + 魔数 + OOXML 校验 + UUID 命名 + 路径越界校验 | `service/FileStorageService` |

---

## 三、未实现清单（明确差什么）

### 3.1 功能缺口

| # | 缺什么 | 阻塞点 |
|---|---|---|
| 1 | **AI 课件解析（F002）** | 🛑 **需要 DeepSeek API Key**（`DEEPSEEK_API_KEY` 环境变量） |
| 2 | **学生 AI 问答（F004）** | 🛑 同上 |
| 3 | 直播翻页广播（F003） | 🟡 直播推流方案未定（表中 `stream_push_url`/`stream_pull_url` 是预留字段），但 **WebSocket 广播页码本身可先做** |
| 4 | 课后总结（F005）/ 学情统计（F006） | 🟡 依赖 F002/F004 有数据 |
| 5 | ~~前端接真实后端~~ | ✅ **已接通**（2026-09-11）。`USE_MOCK` 已改为默认关，dev 下直连后端 |
| 6 | 前端缺失页面 | 学生端直播页、教师端发起直播页 |

### 3.2 接口缺口（契约已定，尚未实现）

| 接口 | 契约位置 | 状态 |
|---|---|---|
| `GET /api/courseware/{id}/prompt-pack` | 2.5 | ⬜ **不依赖模型，随时可做** |
| `POST /api/qa/ask`、`GET /api/qa/records` | 4 | ⬜ 依赖模型 |
| `POST /api/session`、`GET /api/session/{id}` | 3 | ⬜ 依赖 F003 |
| WebSocket `/ws/page` | 5 | ⬜ |
| `GET /api/courseware/{id}/parse-progress` | 设计文档 | ⬜ 依赖 F002 |

### 3.3 已知技术债与取舍（审查发现，MVP 阶段接受）

| 项 | 说明 | 来源 |
|---|---|---|
| 登录无限流 | 可被暴力尝试，建议加令牌桶 / 失败锁定 | 安全审查 M2 |
| 用户名枚举 | 注册接口会明确返回「用户名已存在」 | 安全审查 M3 |
| 密码策略偏弱 | 仅 6–64 位，无复杂度要求；BCrypt 72 字节截断未处理 | 安全审查 M4 |
| Token 存 localStorage | 前端存法，受 XSS 影响；后续可改 httpOnly Cookie | 前端审查 H5 |
| 无会话成员关系 | 任何学生可对任意课堂提问、读问答记录（无选课表） | AI 设计审查 M7 |
| 失败页明细缺失 | 解析 `PARTIAL` 只有任务级状态，教师看不到具体哪页失败 | AI 设计审查 M4 |
| 无问答流式输出 | 20 秒内前端空白，可用 SSE 改善 | AI 设计审查 L3 |
| 限流 / 并发控制缺失 | AI 调用无限流，多学生并发会打爆配额 | AI 设计审查 H1 |
| **上传大小上限写在 3 处** | `FileStorageService` 常量、`application.yml`、`GlobalExceptionHandler` 文案，改动易漂移 | 代码审查 L4 |
| 索引未针对模糊搜索优化 | `like '%kw%'` 走不了索引；数据量大后列表会慢 | 代码审查 M2/M3 |
| 无 ESLint / 无前端单测 | 前端目前没有 lint 与测试 | 前端审查 M5 |

---

## 四、技术栈全景

| 层 | 技术 | 版本 | 干什么 | 为什么选它 |
|---|---|---|---|---|
| 前端 | Vue 3 + Vite | 3.5 / 6.0 | 页面与交互 | 主流、上手快 |
| 前端 | Vue Router / Pinia / axios | 4 / 2 / 1 | 路由 / 状态 / 请求 | 生态标配（Pinia 目前基本未用） |
| 后端 | Spring Boot | 3.5.0 | 框架 | 课程要求 |
| 后端 | Spring Security + jjwt | 6 / 0.12.6 | 认证鉴权 | 无状态 JWT，不用 Session/Redis |
| 后端 | Spring Data JPA | 随 Boot | 数据库访问 | 少写 SQL |
| 后端 | Apache POI | 5.5.1 | 解析 `.pptx` | Java 读 Office 的标准库 |
| 后端 | Lombok | 1.18.48 | 少写样板代码 | ⚠️ JDK 25 下需显式配注解处理器 |
| 数据库 | MySQL | 9.5 | 结构化存储 | 本机已装 |
| 存储 | 本地文件系统 | — | 课件 `.pptx` + 网页幻灯片 | MVP 够用，后续可换对象存储 |
| 大模型 | DeepSeek | `deepseek-flash` | AI 解析 / 问答 | 纯文本、成本低（🛑 Key 待提供） |
| 实时 | WebSocket | 随 Boot | 翻页页码广播 | 双向、低延迟（⬜ 未实现） |
| **未采用** | Redis | — | — | 并发不高，JWT 已解决登录态，**刻意不用** |
| **未采用** | Neo4j | — | — | 「知识图谱」名实分离，用普通外键关联即可 |
| **未采用** | TTS / ASR / 多模态 | — | — | DeepSeek 无这些接口，**已从需求中砍掉** |

---

## 五、配置清单与牵扯关系 ⭐

> 这一节回答：**有哪些配置、各自作用、改了一个会牵动谁。**

### 5.1 后端配置（`backend/src/main/resources/application.yml`）

| 配置键 | 当前值 | 作用 | 改了会影响谁 |
|---|---|---|---|
| `server.port` | `8081` | 后端端口 | ⚠️ **前端 Vite 代理目标必须同步改**，否则前端全部请求 404 |
| `spring.profiles.active` | `${SPRING_PROFILES_ACTIVE:local}` | 默认走 local | 影响加载哪个 `application-*.yml` |
| `spring.datasource.url` | `jdbc:mysql://localhost:3306/friday_class?...characterEncoding=UTF-8...` | 数据库连接 | ⚠️ `characterEncoding` 必须写 **Java 字符集名**（UTF-8），写 `utf8mb4` 会启动失败 |
| `spring.datasource.username` | `root` | 数据库账号 | — |
| `spring.datasource.password` | `${DB_PASSWORD:}` **空占位** | 数据库密码 | ⚠️ **不能写真实密码**（会提交到公开仓库）；真实值在 `application-local.yml` 或环境变量注入 |
| `spring.jpa.hibernate.ddl-auto` | `none` | 不让 JPA 改表 | 表结构**只由 `db/schema.sql` 管**；改成 `update` 会造成「代码改一个字，库跟着变」的失控 |
| `spring.jpa.open-in-view` | `false` | 关闭 OSIV | ⚠️ 关掉后**懒加载关联必须在事务内取**，否则抛 `LazyInitializationException`。查询里要 `join fetch` |
| `spring.servlet.multipart.max-file-size` | `50MB` | 单文件上限 | 与 `FileStorageService` 的常量、`GlobalExceptionHandler` 的提示文案**三处重复**，改一处要改三处 |
| `jwt.secret` | `${JWT_SECRET:}` **空占位** | JWT 签名密钥 | ⚠️ 真实值在 `application-local.yml`（≥32 字节，否则启动报错）。**改了它 → 所有人现有令牌立即失效** |
| `jwt.expire-ms` | `604800000`（7 天） | 令牌有效期 | 改短更安全、改长更省登录 |
| `deepseek.api-key` | `${DEEPSEEK_API_KEY:}` **空占位** | 大模型 Key | 🛑 **尚未提供 → F002/F004 做不了** |
| `deepseek.model` | `deepseek-flash` | 模型 ID | ⚠️ 服务端**只有** `deepseek-flash` / `deepseek-v4-pro` 两个 ID，写 `deepseek-v4-flash` 会每次调用 400 |
| `app.storage.root` | `${STORAGE_ROOT:./storage}` | 课件与幻灯片落盘根目录 | ⚠️ 改了要保证目录可写；该目录已在 `.gitignore` 中 |
| `logging.level.com.fridayclass` | `debug` | 日志级别 | 上线前建议降为 `info` |

### 5.2 本地私密配置（`application-local.yml`，**已 gitignore，不进仓库**）

| 配置键 | 作用 |
|---|---|
| `spring.datasource.password` | 本机 MySQL 密码 |
| `spring.jpa.show-sql` | 打印 SQL（仅本地） |
| `jwt.secret` | 开发用 JWT 密钥 |

> **设计意图**：任何真实密钥都**不进公开仓库**。这个文件被 `.gitignore` 忽略，克隆仓库的人要自己建。

### 5.3 前端配置

| 文件 | 配置 | 作用 | 牵扯 |
|---|---|---|---|
| `frontend/vite.config.js` | `server.port: 5173` | 前端 dev 端口 | — |
| `frontend/vite.config.js` | `proxy /api → http://localhost:8081` | 把 API 请求转发给后端 | ⚠️ **必须与后端 `server.port` 一致** |
| `frontend/vite.config.js` | `proxy /slides → http://localhost:8081` | 转发网页幻灯片 | ⚠️ 后端**任何不以 `/api` 开头**的对外路径都要在这里补一条，否则会被 SPA 兜底吃掉 |
| `frontend/src/api/mock.js` | `USE_MOCK` | 是否用假数据 | **默认关**（`VITE_USE_MOCK === 'true'` 才开）。要看假数据就建 `frontend/.env.local` 写 `VITE_USE_MOCK=true` 并**重启** dev server |
| `frontend/src/api/http.js` | `baseURL: '/api'` | 请求前缀 | 配合上面的代理 |
| `frontend/src/api/http.js` | 401 处理 | 令牌过期自动跳登录 | 依赖后端在**认证失败时返回 HTTP 401** |

### 5.4 配置之间的牵扯关系（看图更清楚）

```
┌──────────────────────────── 前端 ────────────────────────────┐
│  vite.config.js  proxy: /api ─────────┐                      │
│  http.js         baseURL: /api ───────┤                      │
│  vite.config.js  proxy: /slides ──────┤                      │
│  mock.js         USE_MOCK(默认关)     │                      │
└───────────────────────────────────────┼──────────────────────┘
                                        │ 必须一致 ⚠️
                                        ▼
┌──────────────────────────── 后端 ────────────────────────────┐
│  server.port = 8081 ◄─────────────────┘                      │
│                                                              │
│  SecurityConfig 白名单 ──► 哪些接口免登录                     │
│         │                     ▲                              │
│         │                     └── 前端 router 的             │
│         │                         meta.requiresAuth 要对应    │
│         ▼                                                    │
│  jwt.secret ──► JwtService ──► 令牌有效性                     │
│                      ▲                                       │
│                      └── user.token_version（改密码即失效）   │
│                                                              │
│  spring.datasource.* ──► MySQL friday_class（9 张表）         │
│         ▲                                                    │
│         └── 密码来自 application-local.yml（gitignore）       │
│                                                              │
│  app.storage.root ──► FileStorageService ──► 落盘             │
│         │                     │                              │
│         │                     └──► courseware_page.slide_url  │
│         │                                    │               │
│         └──► SlideController 映射 /slides/** ◄┘ 必须对应      │
│                                                              │
│  deepseek.* ──► 【F002 / F004】🛑 Key 未提供                  │
└──────────────────────────────────────────────────────────────┘
```

### 5.5 一句话记住三条最容易踩的

1. **端口 8081**：后端与前端代理必须同时是 8081（8080 被本机 nginx 占着）。
2. **密钥三不入库**：数据库密码、JWT 密钥、DeepSeek Key —— **真实值只在 `application-local.yml` 或环境变量**，公开文件里永远只写 `${...}` 占位。
3. **改了 `jwt.secret`**：所有已登录用户的令牌立即作废（这是预期行为，不是 bug）。

---

## 六、数据库（9 张表）

| # | 表 | 存什么 | 谁写它 |
|---|---|---|---|
| 1 | `user` | 用户（教师/学生） | 注册、种子脚本 |
| 2 | `courseware` | 课件 | F001 上传 |
| 3 | `courseware_page` | 课件页 + 每页文字 + 网页幻灯片地址 | F001 上传 |
| 4 | `knowledge_point` | 知识点（挂在页上） | ⬜ F002 |
| 5 | `preset_question` | 预置提问（挂在页上） | ⬜ F002 |
| 6 | `class_session` | 一次直播课，含 `current_page` | ⬜ F003 |
| 7 | `qa_record` | 学生问答记录，含 `status` | ⬜ F004 |
| 8 | `course_summary` | 课后总结 | ⬜ F005 |
| 9 | `ai_parse_task` | 解析任务进度 | ⬜ F002 |

### 状态机

| 对象 | 状态流转 |
|---|---|
| 课件 `courseware.status` | `UPLOADED` → `CONVERTING` → `CONVERTED` → `PARSING` → `PARSED`；出错 → `FAILED`。**当前上传完成停在 `CONVERTED`** |
| 解析任务 `ai_parse_task.status` | `PENDING` → `RUNNING` → `SUCCESS` / `PARTIAL` / `FAILED` |
| 课堂 `class_session.status` | `NOT_STARTED` → `LIVE` → `ENDED` |
| 问答 `qa_record.status` | `SUCCESS` / `FAILED` |

---

## 七、接口清单（已实现 9 个）

| 方法 | 路径 | 鉴权 | 说明 |
|---|---|---|---|
| POST | `/api/auth/register` | 公开 | 注册（**只能建学生**，不接受 role 字段） |
| POST | `/api/auth/login` | 公开 | 登录，返回 JWT |
| GET | `/api/auth/me` | 需登录 | 当前用户 |
| PUT | `/api/auth/password` | 需登录 | 改密码（**旧令牌全部失效**） |
| GET | `/api/courseware` | 公开 | 课件列表（分页 + 关键字 + 状态筛选） |
| POST | `/api/courseware/upload` | **仅教师** | 上传 .pptx，解析并建页 |
| GET | `/api/courseware/{id}` | 公开 | 课件详情 |
| GET | `/api/courseware/{id}/pages` | 公开 | 课件页列表（含 `slideUrl`） |
| GET | `/slides/{id}/page{n}.html` | 公开 | 网页幻灯片（带 `sandbox` CSP） |

> 详细字段见 `docs/api-contract.md`。**新增接口默认需登录**，要公开必须显式加白名单。

---

## 八、目录地图

```
Friday Class(MVP)/
├── 项目看板.md              ← 本文件（项目总览）
├── README.md                项目简介
├── CLAUDE.md                项目宪章（选题、定调、硬性约束）
│
├── backend/                 后端
│   ├── README.md            ← 后端阅读指南
│   ├── pom.xml              依赖
│   ├── storage/             课件与幻灯片落盘（gitignore）
│   └── src/main/
│       ├── java/com/fridayclass/  （50 个类，见 backend/README.md）
│       └── resources/
│           ├── application.yml          主配置
│           └── application-local.yml    本地私密配置（gitignore）
│
├── frontend/                前端（Vue3 + Vite）
│   └── src/
│       ├── api/             http 封装 + mock 数据
│       ├── pages/           6 个页面
│       ├── layouts/         顶栏布局
│       └── router/          路由 + 登录守卫
│
├── db/
│   ├── schema.sql           建表脚本（9 张表）
│   └── seed_teacher.sql     教师账号预置脚本
│
└── docs/
    ├── api-contract.md      接口契约（前后端合同）
    ├── ai-agent-design.md   AI 智能体设计（V2）
    ├── 周五课堂_架构设计图.docx / _AI智能体设计.docx   交付文档
    ├── *.puml / *.png       设计图源文件与渲染结果
    └── gen_diagram.py / gen_*.py   文档生成脚本（本地、不联网）
```

---

## 九、怎么继续

### 马上能做的（不依赖任何外部资源）

1. ~~前端接真实后端~~ ✅ **2026-09-11 已完成**：mock 默认关，dev 下直接打后端。
2. **prompt-pack 接口**：契约 2.5 已定义，不依赖模型，做完可让学生端提前缓存。
3. **课件详情页加载真实幻灯片**：`CoursewareDetailPage.vue` 目前只把 `slideUrl` 当文本显示，还没用 `<iframe>` 渲染——`/slides` 代理已铺好，接上即可。

### 等 DeepSeek Key 到了才能做

3. **F002 解析智能体**：异步逐页调模型抽知识点 → 写 `knowledge_point` / `preset_question`。
4. **F004 学生问答**：按 `pageId` 组装提示词 → 调模型 → 写 `qa_record`。

### 独立可做

5. **F003 翻页广播**：WebSocket + `class_session.current_page` 落库（直播推流方案另说）。

---

## 十、这个看板怎么用

| 你想知道… | 看哪一节 |
|---|---|
| 这个功能做了没有 | 第二节「功能总览」 |
| 还差什么 | 第三节「未实现清单」 |
| 为什么用这个技术 | 第四节「技术栈全景」 |
| **某个配置改了会怎样** | **第五节「配置清单与牵扯关系」** |
| 表结构 | 第六节 + `db/schema.sql` |
| 接口要带什么参数 | `docs/api-contract.md` |
| 代码怎么读 | `backend/README.md` |
